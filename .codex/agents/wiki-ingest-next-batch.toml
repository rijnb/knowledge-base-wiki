name = "wiki-ingest-next-batch"
description = "Dispatched by wiki-ingest to claim and process one batch during a parallel Wiki import. Do not invoke directly — use wiki-ingest to start an import."
developer_instructions = "Use the repository skill(s) `wiki-ingest-next-batch`, `wiki-ingest-per-note` before doing the work. Follow the wiki-ingest-next-batch skill to claim the next available import batch and process it."
