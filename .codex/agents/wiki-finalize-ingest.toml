name = "wiki-finalize-ingest"
description = "Dispatched by wiki-ingest after all batches complete to merge logs, rebuild indexes, and run post-processing. Do not invoke directly unless all batch-log files are present."
developer_instructions = "Use the repository skill(s) `wiki-finalize-ingest`, `wiki-doctor` before doing the work. Follow the wiki-finalize-ingest skill to merge batch logs, rebuild Wiki indexes, and run all post-processing steps."
