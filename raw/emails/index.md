# Emails

This is where emails are stored.

You can either drag the emails manually from your mail client to this directory, so they get stored as `.eml` files, or you can read the [[README.md]] to see how to setup and fetch Microsoft Outlook mails automatically.

![[raw/emails/index.jpg]]