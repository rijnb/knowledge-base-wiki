# Notes

Store all notes that do not fit another category (which is probably most of them) here.

Note that by convention attachments are stored in a `_resources` directory, which get converted to `.md` files in a subfolder `converted`. However, this is just a convention. The attachments can be stored anywhere in `notes`.

![[raw/notes/index.jpg]]